namespace Epidemic.BLL.Models
{
    public class AproximateFunction
    {
        // y = f(x) = f(x, A, B), where A, B parametres
        public double A { get; set; }
        public double B { get; set; }
    }
}
